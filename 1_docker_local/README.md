# setup a local nginx with docker and terraform
Taken from the main tutorial: https://developer.hashicorp.com/terraform/tutorials/aws-get-started/install-cli

test the CLI setup and run a docker locally.